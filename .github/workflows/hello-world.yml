name: Use Secrets in Workflow

on:
  push:
    branches:
      - main
jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
    - name: Print a Secret
      run: | 
       echo "The API key is: $API_KEY"

      
